#include "main.h"

/**
 * mul - multiplies two integers
 * @a: integer to be multiplied
 * @b: integers to be multiplied
 * Return: Returns 0
 */
int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
