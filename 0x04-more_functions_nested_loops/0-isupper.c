#include "main.h"

/**
 * _isupper - Checks for uppercase alphabets
 *@c: character to be checked
 * Return: Returns 1 when upper and 0 otherwise.
 */

int _isupper(int c)
{
        if (a >= 'A' && a <= 'Z')
        {
                 return (1);
        }

        else
        {
                return (0);
        }
}
